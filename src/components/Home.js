import React from "react"

import Navbar from './Navbar';

const Home = ()=>{
    return(
        <div className="App">
        <header className="App-header">
         <Navbar/>
        </header>
      </div>
    );
}

export default Home